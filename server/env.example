# Server Configuration
PORT=3001
HOST=localhost

# Client Configuration (for CORS)
# Development port
CLIENT_URL=http://localhost:5173
# Note: The server now automatically allows both ports 5173 (dev) and 4173 (build)
